Phase P0 ("Design tokens + brand shell") is implemented, user-verified, and compressed
into `done/P0-design-tokens-brand-shell.md`. `full-plan.md`'s P0 row is `done`.

P1 ("Idea intake screen") is not yet detailed to implementation grade — no
`phases/P1-*.md` exists. Before it can be implemented, a review+advance session must
run Primer A from `AGENT-INSTRUCTIONS/PLANNING/IMPLEMENTATION-PRIMERS.md`: shape P1's
outcome/boundaries/tradeoffs against current code (`frontend/index.html`,
`frontend/style.css` as P0 left them), write `phases/P1-idea-intake-screen.md` from
`full-plan.md`'s existing P1 scope section, run `phase-spec-auditor` over it, then flip
P1's status row to `active` and rewrite this file with Primer B filled in for P1.
