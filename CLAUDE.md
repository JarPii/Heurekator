@AGENTS.md

The instructions for this repo live in `AGENTS.md` (imported above) and in
`AGENT-INSTRUCTIONS/`. This file exists so the Claude CLI picks them up automatically;
keep the content in `AGENTS.md` so every harness reads the same thing.
